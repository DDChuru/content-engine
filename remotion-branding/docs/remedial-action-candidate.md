# Remedial Action — narrated review candidate

Status: **UNAPPROVED review candidate; Claude independent gate and Daniel sign-off pending.** No inventory approval, publication, deployment or git commit is performed. The original approved storyboard remains byte-identical. The original 286-second silent preview and its final/inventory refusal guards remain intact; the new isolated candidate has its own evidence and measured-audio preflight.

Candidate entry: `src/index-remedial-candidate.ts`, composition `RemedialActionTutorial-REVIEW-CANDIDATE`. Reproduce validation with `node tools/verify-remedial-candidate.cjs`; render with `--render` only to an absent candidate MP4. Rendering never calls TTS or writes inventory.

## Evidence and factual treatment

Twenty new unmodified 720×1600 app captures are registered in `public/remedial-tutorial/provenance/candidate-evidence.json`, alongside the twelve pinned Cleaning captures from wave 1. Every reused Cleaning byte and original provenance declaration is still checked by the original contract. Candidate screenshots, state snapshots, narration recordings, receipts, transcript words, fonts and music are hash-verified before metadata and direct component rendering. Candidate data or audio cannot confer approval.

The user explicitly authorized completion of Bakery Demo examples through genuine app flows. On 7 September 2026, the existing session restored as `demo@sunbakebread.co.za`, user `q57f3s9apf4x8vfkzx01896n2588xwqw`, role `site_admin`, with only Bakery Demo `k57ae8hn0kgercz41hgy6s03mn88fzwd` in `siteIds`. Mobile source remained clean at `6999d8eca52fc4f4fff69066dce365f54a609663`; the inspected backend checkout/reference remained `977fb028973b7a9e02603c9b4651e57935dd119b` / `d44f26ab6d6ae823b8c38029d126e650a69f76c9`. The app connected to `dapper-wren-336.convex.cloud`; no claim is made that this deployment's complete source equals the local reference.

All writes used normal UI controls. Diagnostic reads used RKStorage in memory and whitelisted read-only queries through the app's existing authenticated Convex client. No credentials were extracted, no app state/records injected, and no timestamps or device clock altered. Other-site legacy cache keys were observed but their data was not used or mutated. The snapshot register contains scoped identifiers, actual persisted states and real times. Public snapshots omit transient photo URLs and local device photo paths.

| Case | Inspection / finding | Actual result |
| --- | --- | --- |
| A, Major, Required mode | `hx7z4ftsqymx4tjt0jjd0h1e798dzed4` / `d9b3b8db-9ec8-465e-8d90-7cbb4b90c719` | Real required-after alert; genuine after attachment; accepted and closed. Simulated action text, before/after storage IDs and verification survive a fresh read. |
| B, Minor, Optional mode | `hx7z5fqzx0exme1q8ybv7znrfd8dzk3k` / `92c99484-4afd-47a3-8672-309eb561978b` | Rejected with a training-only reason; remains open. |
| C, Major, Optional mode | Same B/C inspection / `8ff363de-b97f-4d25-88e8-4d4941e193d0` | Explicitly escalated to linked `NCR-2026-00044`, NCR ID `rx77bgfv0767hcze58sn8csxgx8dy0tt`. |

Before all decisions, Bill of Health and the shared queue show 3 open Inspection findings. After A accepts and C escalates, Bill shows 1 pending Inspection finding and 24 open NCRs (previously 23). B remains open. The retained demo records and linked NCR are intentional; no cleanup or rollback is claimed.

Every Inspection example uses the normal Android back-camera, annotation, finding, completion, signature and verification flows. The camera subject is the emulator's visible Android test scene. This demonstrates genuine app persistence with synthetic training content; it does not establish a workplace defect, physical attendance, inspection or correction. The signature pad was filled with the literal mark DEMO under `Bakery Demo training operator`; it is not Daniel's or another named person's signature. The actual stored inspector and verification actor remain `Bakery Demo Site Manager`.

There was **no existing open aged Inspection record** to film. The candidate explicitly shows the real `today` badge and says it is **not a later-day comparison**. The original `later-day` evidence slot is not filled or silently aliased. The candidate's independent requirements substitute an honestly named `age-current` view under the user's authorization to adapt wording to available evidence. Carry-over across dates and 8/31-day colour thresholds remain source-backed explanation; the age diagram is labelled “explanatory graphic — not an app screen.” No aged screen, elapsed days or history is fabricated.

The narration retains the scheduled-Cleaning distinction: capturing a failed check can satisfy its occurrence while the finding remains open; verification does not rewrite Fail as Pass. Cleaning attribution is explained as stored data while explicitly acknowledging that both actors and times are not displayed together on the current mobile screen. No UI fields are composited. Inspection Reject remains open; explicit NCR is a separate formal workflow. No owner, due date, frequency, SLA, reminder or automatic Inspection escalation is invented. Reopen is not demonstrated.

## Capture issues retained transparently

The first B/C draft was created in Optional mode after a UI-driver tap failed to select Required. It was retained and used for B/C, with A subsequently created in visibly confirmed Required mode. An unsaved B form was dismissed and recaptured before saving. No duplicate finding was uploaded. Capture `02-demo-create-required.png` is retained but excluded as Required-mode proof: its pixels show Optional. Capture `16-demo-a-after-required-alert.png` is retained but excluded as alert proof: bottom navigation intercepted the tap. Capture 17 is the real alert after scrolling controls into view. No capture was deleted or overwritten.

The Inspection list shows `0 findings` while the issued detail and server show 2 (B/C) and 1 (A). The actual issued detail is used as proof; this product discrepancy is not hidden by changing pixels. The Required-mode attachment control says “optional,” but the real Accept gate demands after evidence; the candidate explicitly distinguishes those facts.

## Narration and render QA

The established series voice ID is `gYWKdgLtqjPO3D5uDrDP` (series Daniel, provider DC), ElevenLabs `eleven_multilingual_v2`, stability 0.55, similarity 0.8, style 0.35, speaker boost enabled. Selected request text, original MP3 bytes and provider receipts are retained. Replaced takes remain on disk and are excluded from the master.

Twenty-two independently transcribed recordings drive the 24-beat, 30 fps timeline, retaining five-second intro and six-second outro, 0.6-second narration lead and at least 29-frame post-recording gap. Speech is not sped up. Operational cuts use 14 actual measured phrase anchors; every evidence view has at least a two-second hold. The assembled narration peaks at −1.8 dBFS. Automated normalized transcript agreement is at least 93%; ASR differences are retained in the QA report rather than overwritten with the script. New human listening approval remains pending.

Bookends reuse the existing `Blue Sea` / Swoop series music asset, hash `f1928a7b68b79b89c843af517583ddc636773e8c4a354e3b610d42611962d186`, at series volume/fades, with no narration overlap. The original license receipt was not found in this session; current authorization covers established series reuse, not a new license or final publishing clearance.

Output and encoded QA are recorded in `out/remedial-action-candidate-v1/verify-render-identity.json` and `verify-encoded-qa.json`. The complete candidate is 12,782 frames / 426.0667 seconds (7:06.067), longer than the 286-second unrecorded plan because timing now follows the established voice.

## Conductor checkpoint

No git commit, branch switch, deploy, publish, file deletion or inventory edit. Cleaning's worktree and Totem playback were not changed. Only the owned emulator was gracefully stopped after capture to free render resources. Exact attributable source/public paths and complete before/after repository status are written to the candidate output's `verify-attributable-paths.json` and `verify-git-status.txt` after final QA.

Capture/action/read/voice preparation scripts and original photo bytes remain under `/tmp/verify-remedial-wave2/`, all scripts named `verify-*`. The production candidate renderer and guarded manifests are self-contained in this repository; no scratch script, backend access or TTS is needed to reproduce the rendered candidate from retained assets.
